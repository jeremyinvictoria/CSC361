To run the program, dpkt module is required, python version 3.0+
1. There are two python file, traceForWindows for Windows, traceForLinux for Linux
you may need to test separately
2. Only trace files end with .pcap are accepted
3. The program DOESN'T work on fragment analysis, such as count number of fragment, and fragment offset
